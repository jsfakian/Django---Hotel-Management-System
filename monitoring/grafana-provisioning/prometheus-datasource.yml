apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    isDefault: true
    url: http://prometheus:9090
    jsonData:
      prometheusVersion: "2.40.0"
      cacheLevel: "High"
      defaultRegex: '/.*/'
